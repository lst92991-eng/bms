# 人工确认记录

本文记录当前硬件/文档证据中无法完全闭环、但已由用户明确确认的项目事实。后续如果原理图、网表或规则文件更新，应同步回填到对应源文件，并保留本记录作为变更依据。

| 日期 | 确认项 | 用户确认 | 影响范围 |
| --- | --- | --- | --- |
| 2026-07-08 | BQ76952 低边电流采样电阻 R18 | `5mΩ` | BQ CC Gain、Capacity Gain、OCC/OCD/SCD 阈值、电流标定、SOC 库仑积分 |
| 2026-07-10 | SC8815 控制板 VBATS 分压 R17/R18 | `R17=200kΩ`（BMS+ 到 VBATS），`R18=10kΩ`（VBATS 到 GND） | 外部分压名义充电目标改为 `25.2V`；`VBAT_SET=0x20` 保持外部分压模式，实测截止电压仍需上板确认 |
| 2026-07-12 | BQ76952 I2C CRC 项目默认值 | 当前硬件与实板通信按 `CRC disabled` 作为默认基线；INT 层继续保留 CRC 支持和切换能力 | 更新硬件规则、项目文档和 AI 任务说明；不得再把“默认开启 CRC”作为当前事实 |
| 2026-07-12 | 新版硬件 BQ WAKE/TS2 外部唤醒通道 | 新版硬件已删除该通道；它不是普通人机按键，软件不得依赖或驱动 `PB3` 完成 BQ 唤醒 | 更新硬件规则、接口预留和冲突清单；保留 shutdown/充电器唤醒的实际闭环测试 |
| 2026-07-12 | 放电链路验证状态 | 完整放电测试已通过，可作为当前阶段合格基线；后续只做必要回归，不重复扩大范围 | 后续主要测试资源转向充电曲线、充电截止、温度和保护阈值验证 |
| 2026-07-12 | 看门狗策略 | 当前阶段暂不加入 IWDG/WWDG，状态标记为 `Deferred`，不作为本轮闭环阻塞项 | 文档保留风险说明，但本轮不生成实现任务 |
